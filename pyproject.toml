[project]
name = "tgv-docs"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "weasyprint>=62.0",
    "markdown>=3.7",
    "pyyaml>=6.0",
    "jinja2>=3.1",
    "cairosvg>=2.8.2",
]

[dependency-groups]
dev = [
    "hypothesis>=6.0",
    "pytest>=9.0.2",
]
